spring:
  application:
    name: inscritos
  datasource:
    url: jdbc:postgresql://localhost:5432/projects
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true